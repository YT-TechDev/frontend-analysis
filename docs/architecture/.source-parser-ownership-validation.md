# Source Parser Ownership Validation Notes

This temporary file must not merge. It exists only to prevent opening a Pull
Request before the canonical documentation map and ADR index include the new
contract. Remove it before PR creation.
